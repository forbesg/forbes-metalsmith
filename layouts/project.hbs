{{> head}}
<div class="container-fluid banner project-page">

  <div class="">
    <header>
      <div class="row">

        {{> header}}

      </div>
    </header>
    <div class="container">
      <div class="banner-text">
        <h1 class="title">{{title}}</h1>
        <p >
          {{subhead}}
        </p>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <h2>{{title}}</h2>
  </div>
  <div class="row">
    <div class="col-md-6">
      <img src="{{image}}" alt="{{title}} Screenshot" class="img-responsive" />
    </div>
  </div>


    {{{ contents }}}
</div>
{{> footer}}
